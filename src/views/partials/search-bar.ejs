
<div class="row g-2">
    <div class="col-md-10">
        <input
            type="text"
            class="form-control"
            name="<%= search_name %>"
            placeholder="Nhập từ khóa..."
            value="<%= keyword || '' %>"
                    >
            </div>

            <div class="col-md-2 d-grid">
                    <button class="btn btn-primary">
                        Tìm kiếm
                    </button>
                </div>
        </div>